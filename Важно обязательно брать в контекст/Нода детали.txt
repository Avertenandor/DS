Давай разберём по полочкам каждую строчку:

---

### 1) `wss://old-patient-butterfly.bsc.quiknode.pro/4f77305d4e6f7ce51cace16a02b88659c7ec249d`

* **Что это?**
  WebSocket-URL твоей QuickNode-ноды на BSC.
* **Зачем?**
  Позволяет подписываться на «пуши» — новые блоки, логи событий, pending-транзакции в реальном времени, без постоянного опроса.
* **Как подключаться?**

  ```python
  from web3 import Web3

  w3_ws = Web3(Web3.WebsocketProvider(
      "wss://old-patient-butterfly.bsc.quiknode.pro/4f77305d4e6f7ce51cace16a02b88659c7ec249d", 
      websocket_timeout=30
  ))
  assert w3_ws.is_connected()
  ```
* **Когда применять?**
  Если нужно «живьём» слушать новые события (`eth_subscribe("logs", {...})`), например для бота или стриминга.

---

### 2) `https://old-patient-butterfly.bsc.quiknode.pro/4f77305d4e6f7ce51cace16a02b88659c7ec249d`

* **Что это?**
  HTTP-RPC endpoint той же ноды.
* **Зачем?**
  Для классических JSON-RPC вызовов: `eth_getBlockByNumber`, `eth_getLogs`, `eth_call` и т. д.
* **Как подключаться?**

  ```python
  import requests
  from web3 import Web3

  session = requests.Session()
  session.timeout = (10, 30)

  w3_http = Web3(Web3.HTTPProvider(
      "https://old-patient-butterfly.bsc.quiknode.pro/4f77305d4e6f7ce51cace16a02b88659c7ec249d",
      request_kwargs={'session': session}
  ))
  assert w3_http.is_connected()
  ```
* **Когда применять?**
  Большинство твоих парсинговых и аналитических скриптов — именно через HTTP, с ретраями, адаптивным чанкингом и т. д.

---

### 3) `4f77305d4e6f7ce51cace16a02b88659c7ec249d`

* **Что это?**
  Твой **API-ключ** (в QuickNode называют «Request ID»).
* **Зачем?**
  Это часть URL-пути — QuickNode связывает его с твоим аккаунтом, лимитами и биллингом.
* **Как не потерять?**

  * **Никогда** не выкладывать в открытый GitHub.
  * Хранить в переменных окружения (`os.environ["QN_KEY"]`) или в `.env`.
  * В коде подставлять через f-строку:

    ```python
    KEY = os.getenv("QUICKNODE_KEY")
    URL = f"https://blissful-smart-diagram.bsc.quiknode.pro/{KEY}/"
    ```

---

## Итог

* **WebSocket (wss\://…)** — real-time подписки и стриминг.
* **HTTP (https\://…)** — классический JSON-RPC, сбор исторических данных.
* **API-ключ** — уникальный идентификатор твоей ноды, даёт доступ и учитывает расход квот.

С этой связкой можешь:

1. **Подписываться** на новые блоки и логи в режиме реального времени.
2. **Опросить** историю и собрать Transfer-события через HTTP-RPC.
3. **Контролировать** расход (через QuickNode Dashboard) и при необходимости менять ключ/субдомен.

Теперь ты чётко понимаешь, что и зачем используется в твоём проекте!🚀
  



  **Ограничения плана «Build» за \$49/мес (QuickNode)**

1. **API Credits**

   * Включено **80 000 000** API-кредитов в месяц; сверх этого — доплата **\$0.62** за каждый миллион кредитов ([g2.com][1], [g2.com][2]).

2. **Rate Limit**

   * **50 запросов в секунду** (RPS) по всем методам RPC ([g2.com][1], [g2.com][2]).

3. **Потребление кредитов за метод**

   * Разные методы списывают разное число кредитов. Например:

     * `eth_getLogs` — **6** кредитов за вызов ([blog.quicknode.com][3]).
     * `eth_call`, `eth_sendRawTransaction` — **1** кредит за вызов.
     * Методы трассировки (`debug_traceBlockByNumber` и др.) — до **20** кредитов.

4. **Streams (WebSocket-стримы)**

   * **10 GB** исходящего трафика стримов в месяц; сверх — по credit-ставке ([g2.com][1], [g2.com][2]).

5. **Functions (Serverless-функции)**

   * **500 GB-секунд** выполнения функций в месяц; сверх — по credit-ставке ([g2.com][1], [g2.com][2]).

6. **IPFS-хранилище**

   * **10 GB** места для IPFS; сверх — по credit-ставке ([g2.com][1], [g2.com][2]).

7. **Payload-размер и `getLogs`**

   * Ответ одного RPC-запроса ограничен по размеру. Если «payload» слишком большой, вернётся ошибка `-32616 Payload too large`.
   * Рекомендуется разбивать `eth_getLogs` на чанки (\~1000 блоков), чтобы не превышать лимиты ([quicknode.com][4]).

8. **Количество endpoint’ов**

   * В бесплатном плане — **1 endpoint**; в Build-плане фиксированного ограничения по числу endpoint’ов **нет** (неограниченно) ([g2.com][1]).

9. **SLA и надёжность**

   * Для платных планов действует SLA **99.99%** uptime ([quicknode.com][5]).

10. **Безопасность и управление доступом**

    * С Build-плана доступны расширенные возможности конфигурации endpoint’ов: JWT-аутентификация, whitelist по IP или Referrer, domain masking и др. ([quicknode.com][6]).

---

💡 **Рекомендация:**
При построении аналитики учитывайте не только RPS, но и стоимость API-кредитов для «тяжёлых» методов (`eth_getLogs`, трассировка). Разбивайте крупные запросы на мелкие чанки, чтобы избежать ошибок payload и оптимально расходовать кредиты.

[1]: https://www.g2.com/products/quicknode/pricing "QuickNode Pricing 2025"
[2]: https://www.g2.com/products/quicknode/pricing?utm_source=chatgpt.com "QuickNode Pricing 2025"
[3]: https://blog.quicknode.com/build-more-quicknode-faqs/?utm_source=chatgpt.com "Build more with QuickNode - FAQs"
[4]: https://www.quicknode.com/docs/ethereum?utm_source=chatgpt.com "QuickNode Ethereum RPC Overview"
[5]: https://www.quicknode.com/core-api?utm_source=chatgpt.com "Core API - The Solution for Blockchain Apps - QuickNode"
[6]: https://www.quicknode.com/docs/stacks?utm_source=chatgpt.com "QuickNode Stacks REST API endpoints"
